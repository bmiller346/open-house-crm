import React from 'react';
import { Box, Typography } from '@mui/material';

const NavPanel: React.FC = () => {
  return (
    <Box sx={{ p: 2 }}>
      <Typography variant="h6">Navigation Panel</Typography>
    </Box>
  );
};

export default NavPanel;
